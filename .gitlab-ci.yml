image: alpine:latest

pages:
  stage: deploy
  script:
    - apk add --no-cache hugo
    - hugo --baseURL "https://oliveandj.com/"
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH

